spring:
  profiles:
    active: local

---
spring:
  profiles: local
  jpa:
    hibernate:
      ddl-auto : create-drop # update
    show-sql: true
  datasource:
    platform: h2
    h2:
      console:
        enabled: true